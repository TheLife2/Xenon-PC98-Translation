# 변경 기록

## 2026-08-10 (2) — DA1 word 모드 해독 + 시작 로고 크레딧

### word 모드 코덱 해독 (`tools/da1w.py`)
- `.ADT` 안 서브 이미지 89장이 쓰던 미해독 변형을 풀었다.
  **word 89/89 strict 통과**(컬럼 6,080개), **byte 203/203 회귀 픽셀 완전 일치**.
- 정체: emit 이 16비트라 컬럼 수 = `w>>1`. 단 `w`/`x` 가 홀수면 남는 1바이트 컬럼을
  **바이트 모드 디코더로** 처리한다(89장 중 22장이 홀수). 이 혼합이 단순 가설을 전부 깨뜨렸다.
- 역참조 델타 32개를 기계어로 전수 검증 — **원래 표가 맞았다.** 고칠 것이 없었다.
- 줄무늬의 진짜 원인은 **프레임 간 VRAM 미유지**였다. 차분 프레임이 이전 프레임 화면을
  역참조(`D8` 1,619회)하는데 호출마다 빈 평면을 새로 잡고 있었다.
  `decode_full(planes=, mask=)` 로 VRAM 을 이어 주자 완전히 사라졌다.

### 시작 로고 화면에 번역 크레딧 (`tools/gfx_adt_credit.py`)
- **word 인코더를 만들지 않고** 달성했다. AGS.EXE 는 `TEST CS:[0x9a],0x4000` 한 줄로
  디코더를 고르므로, flags 에 bit14 를 세우고 **이미 검증된 byte 인코더**로 다시 쓰면 읽는다.
- `0100B.ADT` 프레임 28: word 10,645 → byte 10,296바이트(0.97배), 픽셀·알파 완전 일치.
  마지막 프레임을 자기완결형으로 만들어 앞 프레임 상태와 무관하게 같은 결과가 나오게 했다.
- 컨테이너 재검증 29/29, 디스크 이미지 반영, 실기 부팅·한국어 메뉴 확인.

### 도구
- `gfx_adt_anim.py` 누적 합성 렌더, `gfx_build.py` 가 ADT 컨테이너도 검증·주입
- `gt_*.py` 정답 화면 수집 도구 6종
- 해독 기록: `gfx/WORD_MODE_RE.md` (반증된 가설과 헛짚은 곳 포함)

## 2026-08-10

### 번역
- 전수 검수 워크플로우(검수 40청크 + 재검 + 종합, 81 에이전트) 결과 **200줄 반영**
  - 오역 38건 (화행·주체 역전 류 포함), 용어 통일 56건, 직역투 40건, 오탈자·형식 29건 등
  - 전역 표기 통일: 《콰드라》 · 에이레이트 · 함선 · 기시감각 · 컴퓨터 룸 · 엔진 룸 · 흉포 · 라든가
  - 보고서: `translation/REVIEW_FULL_20260810.md`, 반영 도구: `tools/apply_review2.py`
- 번역 크레딧 「번역 : THL.kr」 3곳
  - 서시(에피그래프) 화면: 엔진 텍스트 (overrides 1b - 타이틀 블록은 3줄 창이라 불가 실측)
  - 엔딩 로고(ROGO): 로고 마크와 문구 사이 빈 띠
  - 엔딩 저작권 배너(0100BB): 캔버스를 24→48줄로 늘려 아래에 추가

### 그래픽 한국어화 (DA1 자체 코덱)
- 203장 시각 전수 조사(`gfx/TEXT_SURVEY_20260810.md`) 후 17장 교체:
  - `XENON2` 엔딩 타이틀: 肉奴隷/トレイシーの憂鬱 → 육노예/트레이시의 우울 (Noto Serif KR)
  - `MENU1`/`MENU2` 메뉴: 게임 시작·데이터 로드·스페셜 모드 / 레이디스 룸·뮤직 모드
  - `MUSIC` 뮤직 모드, `ST01~ST08` 스태프 롤(역할 한국어 + 인명 한글 표기)
  - `1020`/`1020_3`/`1030` 병원 안내판: 接受→접수, 第2…→제2진…
  - `1010` 책등 소품
- 모든 편집은 지정 영역 밖 원본 보존을 픽셀 단위 기계 검증. 편집본은 `gfx/edit/`
- 한계: 시작 로고 애니메이션(0100B.ADT)은 word 모드 DA1 변형이라 미해독 (89 서브 프레임)

### 도구
- `gfx_font_survey.py`/`gfx_preview_fonts.py`: 원본 획 특성(런렝스 변동계수) 기반 글꼴 정량 비교
  - 라이선스 조사로 Windows 번들(한양) 서체 배제, OFL(Noto/굴림 예외 검토) 채택
- `gfx_signs_ko.py`/`gfx_misc_ko.py`/`gfx_staff_ko.py`/`gfx_credit_banner.py`: 그림별 교체 도구
- `gfx_adt.py`: ADT 컨테이너 목차, `gfx_check.py`: 디스크 이미지 되읽기 검증
- `apply_review2.py`: 검수 결과 일괄 반영 (overrides 15번 섹션 생성 + 병합 + 대조)

### 검증 상태
- 번역 9,467/9,467 (100%), koverify 치명 0건
- 빌드: 스크립트 40/40 디스크 되읽기 일치, 그래픽 17장 되읽기·재디코드 일치
- 에뮬레이터(Neko Project II) 부팅·메뉴 하이라이트·서시 크레딧 실기 확인

## 2026-08-09 이전 (요약)

- 영문 패치 파이프라인 재현(병합·LZSS·HDI 37/37 바이트 일치) 후 한국어 파이프라인 구축
  - 미사용 JIS 구획에 한글 글리프 할당 + 폰트 시트 생성 (`table/kotable.json` append-only)
- 본문 번역 9,467줄 완료, 검수 3회(어체·SFX·전수), 참고 문서화(`TRANSLATION_NOTES.md`)
- DA1 그래픽 코덱 역공학: 디코드 203/203, 인코드 왕복 203/203 (`tools/da1.py`, `tools/da1enc.py`)
